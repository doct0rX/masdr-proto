result entries: 7 labels: ['v2:8b75ad1458fd1984d8cc2bfdf25e61f86b05992a662f066b923c72e509773ad5', 'v2:df2a230d5acc4c208b0e3eda6eb1efd9990275d6a4ab27e106e8205c4549c504', 'v2:8c8d808d1fc448c5a8d0bc72186c46110cb3dee5a7be2995d1eaa710d438c029', 'v2:e097dcc2869e91267420efc6cf0d5b9d0295cbde028e479ba0a3a745f16039cb', 'v2:4726761704c0f9ba8da2d4678a68b609a02a6751518d5b8785ed12d446c5bc1d', 'v2:ac341bce221c0fb55523bdf58df742944a371632b4dfb2a9cc12a741e427781a', 'v2:1d1c8597587986066e62714c083e02ce3701e60ddd6aa5ae32573bf73540a247']

############ v2:8b75ad1458fd1984d8cc2bfdf25e61f86b05992a662f066b923c72e509773ad5
SUMMARY: Verified against live docs on 2026-09-02: all four Anthropic models (Fable 5.1, Opus 5, Sonnet 5, Haiku 4.5), OpenAI GPT-5.6 Sol/Terra/Luna, and Gemini 3.7 Flash / 3.1 Pro Preview accept image input and support strict JSON-schema structured outputs, so the choice is driven by vision quality, reliability, latency, cost and API ergonomics rather than capability gaps. Recommendation: primary `claude-opus-5` (Anthropic's own "start here" model, explicitly positioned for vision-heavy workflows, high-res 2576px vision tier, GA structured outputs via `output_config.format` + `messages.parse`, $5/$25 per MTok, 1000 RPM / 2M ITPM even at Start tier) with `claude-sonnet-5` ($2/$10, "Fast", same API surface, no safety-classifier refusals) as the drop-in fallback and as the model for the cheap vision-observation and critic stages. Do not use Fable 5.1 for this: 2x price, "Slower", forced `tool_choice` returns 400, 30-day data retention required, smaller rate-limit bucket (500K ITPM / 100K OTPM at Start), and its vision uplift comes mostly from a crop/zoom tool that also lifts Opus/Sonnet. GPT-5.6 Sol (`gpt-5.6-sol`, $4/$20) is a close second and Gemini 3.7 Flash (`gemini-3.7-flash`, $0.75/$3.75 promo, ~340 tok/s) is the cheapest/fastest option, but Google's Pro flagship is still `-preview` and the Flash promo price ends 2026-12-31. Pipeline: pre-resize image in code -> vision observation (structured, with a `zoom` crop tool) -> deterministic template routing -> rulebook-grounded generation of a base size only (cached system prompt) -> compute grading/derived measurements/colorway matrix in code -> schema + code validators -> LLM critic -> bounded (max 2) repair loop. SO-Bench shows frontier models hit ~97% schema validity but <12% exact-value match on image->JSON, which is the empirical case for computing every number you can in code. SDK: TypeScript `@anthropic-ai/sdk` (0.123.0) with Zod `zodOutputFormat` if the demo is a Node/Next.js web app; Python `anthropic` 1.x with Pydantic `messages.parse` otherwise. Env var: `ANTHROPIC_API_KEY`.
RECOMMENDATIONS:
  - Primary model: `claude-opus-5` for the vision-observation (effort `medium`) and generation (effort `high`) stages; fallback and cheap-stage model: `claude-sonnet-5` (critic/repair at effort `low`, and full pipeline fallback on Opus 429/refusal). Reserve `claude-haiku-4-5` only for trivial enum classification if you need it; skip `claude-fable-5-1` for the prototype and re-benchmark it later at `effort: low` once you have an eval set.
  - Use the Anthropic SDK directly (TypeScript `@anthropic-ai/sdk` + Zod `zodOutputFormat` for a Node/Next.js demo; Python `anthropic` 1.x + Pydantic if post-processing/crop tooling lives in Python). Use `client.messages.parse` with `output_config.format` for every document-shaped output, `strict: true` only for the `zoom` crop tool, and `messages.stream().finalMessage()` for the generation stage.
  - Enable `fallbacks: 'default'` with beta header `server-side-fallback-2026-07-01` on Opus 5 calls so classifier refusals auto-retry on Anthropic's recommended model; always branch on `stop_reason` before reading content.
  - Build the pipeline as prompt chaining with code gates: (0) resize/normalize image in code -> (1) structured VisionObservations with zoom tool -> (2) deterministic merge with user text + template routing -> (3) rulebook-grounded TechPackDraft for base size M only, rulebook + exemplar cached with `cache_control` -> (4) compute grading (S 56 / M 58 / L 60 cm head circ, +/-0.5 cm), derived measurements, colorway matrix and BOM consumption in code -> (5) JSON-schema + business-rule validators, then a Sonnet 5 CritiqueReport -> (6) at most 2 repair iterations, then human review flag.
  - Design the JSON schemas around Anthropic's grammar limits: no numeric min/max, no minLength/maxLength, no recursion; encode allowed values as enums (size codes, stitch types 301/401/504, SPI 8|9|10, Tex 27|40|60, seam classes) and repeat the enums in prompt text; keep the schema byte-stable across requests so grammar and prompt caches hit.
  - Pre-resize photos to <=1568 px long edge (about 1.3-1.5K visual tokens) for the first pass; let the zoom tool pull full-resolution crops only for details (topstitch rows, eyelets, labels). Upload once via the Files API and reference by file_id across stages; keep the user->file_id mapping server-side.
  - Before the demo: purchase Anthropic credits so the org leaves the Evaluation tier, confirm Start-tier limits (1,000 RPM / 2M ITPM / 400K OTPM per model, $500/month cap), run 20-30 warm-up generations to prime prompt caches and surface refusals, and log usage per stage to validate the ~$0.30-0.50 per-pack estimate.
  - Wrap providers behind one adapter so `gpt-5.6-sol` (Responses API, `text.format` json_schema strict, `OPENAI_API_KEY`) and `gemini-3.7-flash` (`response_json_schema`, `GEMINI_API_KEY`) can be A/B-tested on the same eval set; Gemini 3.7 Flash is the obvious candidate if per-pack cost or sub-second TTFT later matters more than quality.
  - Create a 15-30 item eval set now (reversible bucket hat variants, sketches vs photos, Arabic/English descriptions, missing info) with expected observations and measurement tables, and use it to sweep effort levels per stage (Anthropic's guidance: effort is a better lever than switching models).
OPEN QUESTIONS:
  ? Is the prototype a Node/Next.js web app (favors TypeScript SDK + Zod) or a Python service (favors Pydantic + Pillow/OpenCV for the crop tool and post-processing)?
  ? Does the org already have an Anthropic Console account with purchased credits, and which usage tier is it on? (Evaluation-tier limits could break a live demo.)
  ? What is the acceptable end-to-end latency for the demo (e.g. under 60 s with streamed progress vs. several minutes offline)? This decides effort levels and whether the critic/repair loop runs synchronously.
  ? Target cost per generated tech pack and expected demo/beta volume? (Determines whether Opus 5 everywhere, Opus+Sonnet split, or Gemini Flash for some stages.)
  ? Do you have 1-3 real factory-approved tech packs (ideally from Egyptian suppliers) to use as few-shot exemplars and to define the measurement/POM naming conventions the factory expects (cm vs inches, half-measure vs full circumference)?
  ? Must outputs be bilingual (Arabic + English) and/or exported to PDF/Excel? (Affects schema design and whether a rendering stage is needed.)
  ? Is US-only inference / data residency (`inference_geo: 'us'`, 1.1x price) or use of Bedrock/Vertex required for compliance, which would drop Files API and server-side fallbacks?
  ? Should the pipeline accept multiple images per product (front/back/detail) and hand sketches, and how should conflicts between the photo and the text description be resolved (text wins, ask user, or flag)?

############ v2:df2a230d5acc4c208b0e3eda6eb1efd9990275d6a4ab27e106e8205c4549c504
SUMMARY: masdrmena.com is "Masdr" (logo wordmark "MASDR", Arabic "مصدر"; LinkedIn/press: "MASDR MENA"), a Cairo-based (Sheikh Zayed) bootstrapped B2B marketplace that rebranded from "Factory MENA" (announced Mar 2024, relaunched as MASDR in 2026 as "Egypt's first Alibaba-style marketplace"). Founders: Farida Bayoumi (CEO), Adham Magdy/Magdi (Co-founder & COO, Adham@masdrmena.com), Peter Magdi. Tagline "Unlock Egyptian Manufacturing"; buyers are global/diaspora founders and Egyptian local brands, suppliers are vetted Egyptian factories (claimed top 10%); 9 categories incl. Clothing, Footwear, Bags; features are search/browse, chat + custom quote (RFQ), VERIFIED / READY TO SHIP / LOW MOQ badges (MOQ 5-200 pcs), EGP pricing, built-in Arabic-English translation, full RTL Arabic site. No tech-pack, sampling or AI product-development feature exists today, so the prototype fills a real gap; LinkedIn copy does mention "streamlined processes from sampling to mass production". Visual identity is fully recoverable from the live Next.js/Tailwind CSS: deep indigo brand (#251670 logo, #301989 theme-color, #5F50BC primary, #362E83 CTA button), cream/blue-gray neutrals (#F8F6F1, #F1F4F6, #232B32 headings), display font Adrianna, body Satoshi Variable/Onest (Cairo for Arabic), pill buttons (999px radius) with purple glow shadows, 8/12/16px card radii, and a dark glowing-indigo hero.
RECOMMENDATIONS:
  - Name the prototype as a Masdr feature, e.g. "Masdr Tech Pack" / "AI Tech Pack Generator", positioned as the step between browsing factories and 'Chat & request quotes' (Step 2). Add a 'Send to factories for quotes' CTA so it slots into the existing 3-step flow.
  - CSS variables to copy verbatim: --primary #5F50BC; --primary-dark #2A286C; --primary-darker #251670; --purple-main #301989; --primary-light #9E8DFF; --primary-soft #BBB9F9; --cta #FF9369; --cta-hover #FF3000; --link #0CA3C6; --tag-verified #0A9359; --cream #F8F6F1; --blue-gray-50 #F1F4F6; --blue-gray-100 #E3E8ED; --text-heading #232B32; --text-body-dark #2F3943; --border-light #B7A9D2; --radius-sm/md/lg 8/12/16px. Set meta theme-color #301989.
  - Header/hero: dark indigo panel (#0A0A1E -> #251670 radial glow, or a CSS radial-gradient(ellipse at 50% 120%, #9E8DFF 0%, #301989 35%, #000 80%)) inside a 16-20px rounded container with white MASDR wordmark, white 50px display headline with subtle white drop-shadow, and a pill badge (border #BAA8D5, bg rgba(186,168,213,.2)) like "Built for 300,000+ Global Founders & Brand Owners".
  - Buttons: primary = pill, 44px tall, bg #362E83, hover #301989, white 16px medium, glow shadow 0 0 29px #362E83 + inset 0 0 20px rgba(255,255,255,.35); secondary = pill bg rgba(255,255,255,.1) on dark or #F3F3F3 with #5B3FAE text on light; upload dropzone = white pill/rounded-24px card with 0 0 18px rgba(0,0,0,.1) shadow and backdrop-blur.
  - Fonts: use Satoshi (Fontshare) or Inter for UI/body at 14-16px, a geometric display face (Outfit/Manrope 400) for section titles at 24-50px with -1% tracking, Cairo for Arabic, and Geist Mono for spec-table numbers (Geist Mono is already loaded on the site). Do not fake Adrianna.
  - Tech pack pages: white/cream (#F8F6F1) page, sections as #F8F9FB rounded-24px panels, spec tables with #E3E8ED borders, #232B32 header row text, alternating #F1F4F6 rows; colorways shown as swatch tiles on #F2F2F2 with Pantone/hex labels; status chips reuse Masdr badge colours (VERIFIED green #0A9359, READY TO SHIP teal #0CA3C6, LOW MOQ purple #5F50BC, FLASH SALE red #FF3000).
  - Ship EN + AR toggle with dir=rtl, logical CSS properties (padding-inline, border-inline-start) and Cairo font in AR mode, mirroring the real site's locale pill "Egypt (EN/EGP)" / "مصر (عربي/جنيه)"; show MOQ as "MOQ 200 pcs" and cost estimates in EGP.
  - Copy tone: imperative sentence-case headlines, trust vocabulary (verified, factory-ready, mistake-free), no exclamation marks; e.g. 'Turn a sketch into a factory-ready tech pack' / 'Send to verified factories for quotes'.
  - Do not reuse the /en/about placeholder facts (2010, John Anderson, 85 countries) or the /en/contact San Francisco address anywhere in the prototype; use Sheikh Zayed, Cairo and the real founders if attribution is needed.
OPEN QUESTIONS:
  ? Should the prototype be branded as a native Masdr feature (use their logo/wordmark) or as a white-label pitch with Masdr-like styling only? Using the real logo may need their permission for anything shared publicly.
  ? Is the primary user the buyer (brand owner generating a pack before RFQ) or the factory/supplier side (reviewing incoming packs)? The site is buyer-first, but supplier onboarding exists.
  ? Should the output default to EGP costing and Egyptian sizing/units (cm) with Arabic construction notes for factories, or English-only for the first pass?
  ? Do you want the tech pack to end with a 'Request quotes from N matching Clothing factories' handoff into Masdr's chat/RFQ flow (mocked), which is the strongest product-fit story?
  ? Licensing: are you fine substituting Adrianna/Satoshi with open fonts, or do you have access to the licensed files?

############ v2:8c8d808d1fc448c5a8d0bc72186c46110cb3dee5a7be2995d1eaa710d438c029
SUMMARY: A factory-ready apparel/accessory tech pack is a versioned document whose canonical order is: cover/header -> technical flats with callouts -> BOM -> measurement (POM) chart with base size, grade rules and tolerances -> construction/workmanship (ISO 4915 stitch, ISO 4916 seam, SPI, seam allowance, topstitch, pressing) -> colorways / BOM-by-color -> labels, packing and QC/AQL -> fit comments and revision log (sources: Techpacker, Apparel Entrepreneurship, Sewport, Fashion Index, Specter OS headwear guide). For the canonical bucket-hat case the industry numbers are concrete: EN 13402-style head girth S 55-56 / M 57-58 / L 59-60 cm with 1 cm ease and 1-2 cm grade, crown depth 8-10 cm, brim 5-7 cm, 1 cm seam allowance, 301 lockstitch at 10-12 SPI seaming and 7-8 SPI topstitch, Tex 40 poly-core topstitch / Tex 27-30 seaming, tolerances +/-0.5 cm on crown/brim and +/-1.0 cm on circumference, AQL 2.5 major / 4.0 minor at ISO 2859-1 GII. Egypt's domestic market requires Arabic labels (country of origin, manufacturer name, product description) and the mandatory ES 7266 series (7266-3/2023 apparel & home textiles, 7266-4 garments) covering fiber content %, care (ES 1405/2014 = ISO 3758 symbols), origin, manufacturer/importer and restricted chemicals; export markets add EU 1007/2011 fiber naming, US FTC fiber/origin/RN + 16 CFR 423 care, and GSO/SASO Arabic fiber+care+origin. For a prototype, the MUST set is header, front/back/side flat with callouts, BOM with composition/gsm/width/consumption/color ref, POM chart across 3 sizes with cm tolerances, construction notes with stitch/SPI/SA/thread, colorway table with Pantone TCX, label list and packing basics; costing, grading formulas, detailed carton specs and sample-stage sign-off can be NICE placeholders.
RECOMMENDATIONS:
  - Implement the tech pack as the schema above and render it in the canonical order: header -> product description -> flats -> BOM -> POM chart -> construction -> colorways -> labels/packaging -> QC/compliance -> samples/revisions; keep costing and sample log as optional collapsed sections in the prototype.
  - Hard-code the headwear POM library for bucket hats (A inside circumference full-circ, B crown/side-band height, C crown tip diameter, D brim width, E total height, F brim edge circumference, G topstitch offset) with defaults S/M/L = 56/58/60 cm circumference (+/-1.0), 9.0-9.5 cm crown height (+/-0.5), 17.2 cm tip diameter base (+/-0.5), 6.0/6.0/6.5 cm brim (+/-0.5), and let the LLM adjust from the sketch/description rather than invent codes.
  - Encode a construction template per product type: for the bucket hat, 301 lockstitch, 10-12 SPI seaming, 8 SPI single-row brim edge topstitch 5 mm from edge, 10 mm SA, Tex 27-30 seaming / Tex 40 topstitch, needle Nm 80-90, tonal thread per shell, and an explicit reversible-assembly sequence (two complete shells, join at brim edge, turn, close, stitch-in-ditch tack).
  - Always emit color references as Pantone TCX + hex (Khaki 16-0726 TCX #A39264, Jet Black 19-0303 TCX #2D2C2F) and a BOM-by-colorway matrix so the factory can cut CW1 and CW2 from the same two fabric lots.
  - Generate a bilingual Arabic/English care-content label block by default for Egyptian brands: fibre % in Arabic and English, ISO 3758 symbols with Arabic care text (ES 1405/2014), 'صنع في مصر / Made in Egypt', manufacturer name and address, size in cm; flag that ES 7266-3/2023 is obligatory and that labels must be sewn in at the factory (no post-hoc stickers).
  - Include a compact QC block: ISO 2859-1 GII, AQL 2.5 major / 4.0 minor, measurement audit of 10 pcs per size, tests for gsm +/-5%, shrinkage <=3%, colorfastness to wash/rub; plus a proto -> fit -> PP -> TOP sample checklist with approver fields.
  - Mark all factory-dependent values (supplier refs, lab-dip numbers, exact consumption, carton dims) as 'TBC by factory' placeholders instead of hallucinated numbers, and show a confidence/assumption note per section so the user can review before sending to a masdrmena supplier.
  - Validate the generated POM chart programmatically: sizes must increase monotonically by the grade rule, tolerances present in cm, tip diameter consistent with circumference, and every POM code must appear as a callout on a sketch.
OPEN QUESTIONS:
  ? Target markets for the first run: Egypt only, or also GCC/EU/US export? This decides whether the label block needs RN/EN/UK fields beyond Arabic+English.
  ? Does the brand want a brand/main woven label on the reversible hat, and if so on which side (or a double-sided label in the brim seam)?
  ? Should the size run be labelled S/M/L only, or S/M/L with cm (56/58/60) per EN 13402 practice?
  ? Is the 280 gsm twill to be pre-shrunk/sanforized or will the factory pre-wash? This changes the pattern allowance and the shrinkage test requirement.
  ? Preferred units for the factory: cm only, or cm with inches for export buyers?
  ? Will the generator need cost-sheet output (fabric price, CMT, target FOB) in v1, or is it out of scope for the prototype?
  ? Do you want the prototype to output the tech pack as JSON only, or also a rendered PDF/HTML page for the factory?

############ v2:e097dcc2869e91267420efc6cf0d5b9d0295cbde028e479ba0a3a745f16039cb
SUMMARY: Recommended path: one EC2 t4g.small (2 vCPU/2 GiB Graviton, Ubuntu 24.04 arm64) in eu-central-1 (Frankfurt), bootstrapped entirely by cloud-init user-data, fronted by Caddy for automatic Let's Encrypt/ZeroSSL TLS on masdr-proto.thegentek.com, with an Elastic IP, an instance profile (no long-lived keys on the box) for S3 + SSM, SSM Session Manager instead of SSH, SQLite on the root gp3 volume for metadata and S3 for uploads/generated packs, all in Terraform 1.15 with the S3 backend (native lockfile, no DynamoDB). It is the only option that (a) needs no container image (WSL2 has no docker), (b) has no 29s/30s gateway or 6 MB payload ceilings for 10 MB uploads and 60-120 s LLM calls, (c) needs only one manual DNS record at GoDaddy (A record masdr-proto -> EIP), and (d) works identically in eu-north-1 if you stay there. Frankfurt is chosen over Stockholm for ~18 ms better RTT from Cairo (68 ms vs 86 ms measured) and much broader service availability (App Runner, Bedrock EU inference); Stockholm is ~10% cheaper on compute. All-in cost is roughly $20/month in Frankfurt ($18 in Stockholm). Lambda+Function URL (RESPONSE_STREAM, 15 min timeout, 200 MB streamed response) behind CloudFront+ACM is the credible serverless runner-up at ~$1-5/month but adds CloudFront/ACM(us-east-1)/OAC plumbing, presigned-S3 uploads (6 MB sync request cap), and keepalive streaming to survive CloudFront's 30 s (max 120 s) per-packet origin timeout. Fargate needs CodeBuild for image builds plus an ALB (~$40/month total); Amplify Hosting SSR is ruled out by its 30 s compute timeout and 5.72 MB response cap; Lightsail ($12/month flat, available in both regions) is a fine budget fallback but cannot attach IAM roles, so S3 access would need static keys on the box.
RECOMMENDATIONS:
  - Choose Path A: EC2 t4g.small + Caddy + cloud-init, Terraform-managed, in eu-central-1. Rationale: no container build, no gateway timeouts, direct 10 MB multipart uploads through Caddy, SQLite on disk, instance-profile credentials, one A record at GoDaddy, ~$20/month. Keep the same Terraform if you later decide to stay in eu-north-1 (only price and RTT change).
  - Runbook step 0 (account admin, one-off): create IAM user 'masdr-proto-deployer' (or a role assumed via 'aws sts assume-role') with the EC2-path policy listed in findings; 'aws configure --profile masdr' on WSL2; export AWS_REGION=eu-central-1; install session-manager-plugin .deb (sudo dpkg -i session-manager-plugin.deb).
  - Step 1 (Terraform backend): terraform { backend "s3" { bucket = "<existing-bucket>" key = "masdr-proto/terraform.tfstate" region = "eu-central-1" encrypt = true use_lockfile = true } }. Provider aws region eu-central-1, default_tags Project=masdr-proto.
  - Step 2 (Terraform resources): data aws_ssm_parameter ubuntu { name = "/aws/service/canonical/ubuntu/server/24.04/stable/current/arm64/hvm/ebs-gp3/ami-id" }; aws_default_vpc + default subnet; aws_security_group ingress 80 and 443 from 0.0.0.0/0 and ::/0, no 22, egress all; aws_iam_role + aws_iam_instance_profile with AmazonSSMManagedInstanceCore and an inline policy for s3:GetObject/PutObject/ListBucket on the app bucket and ssm:GetParameter on arn:aws:ssm:eu-central-1:<acct>:parameter/masdr-proto/*; aws_s3_bucket 'masdr-proto-data-<acct>' with public access block, SSE-S3, lifecycle expiring uploads/ after 30 days; aws_ssm_parameter '/masdr-proto/ANTHROPIC_API_KEY' type SecureString (pass via TF_VAR, or write with 'aws ssm put-parameter' outside Terraform to keep it out of state); aws_instance t4g.small, ami = data.aws_ssm_parameter.ubuntu.value, root_block_device gp3 20 GB, metadata_options http_tokens = "required", user_data = templatefile("cloud-init.yaml", { domain, bucket, region }); aws_eip + aws_eip_association; output eip.
  - Step 3 (cloud-init.yaml, < 16 KB): #cloud-config; packages: unzip, curl, ca-certificates, debian-keyring, debian-archive-keyring, apt-transport-https, (nodejs via NodeSource 22.x in runcmd, or python3-venv); write_files: /etc/caddy/Caddyfile = 'masdr-proto.thegentek.com { encode gzip; request_body { max_size 20MB }; reverse_proxy 127.0.0.1:8080 }' (add 'basic_auth { user hash }' to gate the prototype); /etc/systemd/system/masdr.service (User=masdr, WorkingDirectory=/opt/masdr/current, EnvironmentFile=/etc/masdr/env, ExecStart=/usr/bin/node server.js, Restart=always, RestartSec=3); /usr/local/bin/masdr-deploy (aws s3 cp s3://<bucket>/releases/latest.zip /tmp; unzip to /opt/masdr/releases/<ts>; npm ci --omit=dev; ln -sfn to /opt/masdr/current; systemctl restart masdr); runcmd: add Caddy cloudsmith repo (curl -1sLf https://dl.cloudsmith.io/public/caddy/stable/gpg.key | gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg; curl -1sLf .../debian.deb.txt > /etc/apt/sources.list.d/caddy-stable.list; apt update; apt install -y caddy awscli), useradd masdr, write /etc/masdr/env from 'aws ssm get-parameter --with-decryption' (chmod 600, PORT=8080, S3_BUCKET, AWS_REGION), /usr/local/bin/masdr-deploy, systemctl enable --now masdr caddy, enable unattended-upgrades.
  - Step 4 (first apply order to avoid burning Let's Encrypt attempts): terraform apply -target=aws_eip.app -> create the GoDaddy A record masdr-proto -> <EIP> TTL 600 (UI, or PUT https://api.godaddy.com/v1/domains/thegentek.com/records/A/masdr-proto with 'Authorization: sso-key KEY:SECRET' and body [{"data":"<EIP>","ttl":600}]) -> upload the first release zip to s3://<bucket>/releases/latest.zip (works with today's S3-only creds) -> terraform apply (full). Caddy retries automatically if DNS is still propagating.
  - Step 5 (verify): dig +short masdr-proto.thegentek.com; curl -I https://masdr-proto.thegentek.com; aws ssm start-session --target <instance-id> then 'journalctl -u caddy -u masdr -n 200' and 'tail /var/log/cloud-init-output.log'.
  - Step 6 (deploy loop, no docker, no SSH): zip -r release.zip . -x 'node_modules/*' '.git/*' && aws s3 cp release.zip s3://<bucket>/releases/latest.zip && aws ssm send-command --document-name AWS-RunShellScript --targets Key=tag:Name,Values=masdr-proto --parameters 'commands=["/usr/local/bin/masdr-deploy"]' ; poll with aws ssm get-command-invocation.
  - App-shape guidance for either path: make POST /generate return a job id immediately and stream progress over SSE (or poll GET /jobs/{id}), persisting the job row in SQLite (EC2) or DynamoDB/S3 JSON (Lambda), so a browser refresh or proxy hiccup during a 120 s generation does not lose the result; write the finished tech pack (JSON + rendered PDF/XLSX) to s3://<bucket>/packs/<job>/ and serve via presigned GET.
  - Robustness add-ons worth their cost: EBS daily snapshot via aws_dlm_lifecycle_policy (needs dlm:* + ec2:CreateSnapshot in the deployer policy), CloudWatch agent for disk/mem alarms, an aws_budgets_budget alert at $30, and IMDSv2-only. Do not run terraform destroy/apply of the instance more than a few times per week (Let's Encrypt duplicate-cert limit 5/week; ZeroSSL fallback exists).
  - If you later want zero-idle-cost: Path B = Lambda (arm64, 2048 MB, 300 s timeout) + Lambda Web Adapter layer (LambdaAdapterLayerArm64:28, AWS_LWA_INVOKE_MODE=response_stream) + Function URL (AWS_IAM, RESPONSE_STREAM) + CloudFront (OAC lambda origin, response timeout raised to 120 s, ACM cert in us-east-1, alias masdr-proto.thegentek.com -> CNAME d<hash>.cloudfront.net at GoDaddy) + presigned S3 PUT uploads (6 MB sync cap) + SSE heartbeats every <=25 s. Skip API Gateway entirely (30 s HTTP API hard cap; REST needs a quota exception).
  - Do not choose Amplify Hosting (30 s SSR timeout, 5.72 MB response cap) or Fargate for this prototype (CodeBuild image pipeline + ALB pushes cost to ~$40/month and IAM surface up 4x with no functional gain at one instance). Lightsail $12/month is acceptable only if the budget, not credential hygiene, is the binding constraint.
OPEN QUESTIONS:
  ? Who in the AWS account can create the new IAM user/role (the current user has no iam:* rights) and do they prefer a long-lived user with access keys or an assumable role with MFA?
  ? Is the LLM called via the Anthropic API directly or via Amazon Bedrock? (Bedrock changes the instance-role policy and makes eu-central-1 a stronger choice; direct API makes region irrelevant to the model.)
  ? Node.js or Python for the app? (Decides NodeSource vs python3-venv in cloud-init and the systemd ExecStart; the Lambda Web Adapter fallback supports both.)
  ? Do you have (or can you get) GoDaddy API keys for thegentek.com, or will the A record be added manually in the GoDaddy DNS UI? Who owns that GoDaddy account?
  ? Should the prototype be gated (Caddy basic_auth or a shared link token) before Egyptian brand users see it, and is there any data-residency requirement for uploaded product photos that would rule out EU regions?
  ? Expected concurrency for demos (a single t4g.small comfortably handles ~10 simultaneous I/O-bound generations; more needs t4g.medium or the Lambda path)?
  ? Is a monthly ceiling of roughly $20-25 acceptable, or is the target closer to $0 idle (which would favour the Lambda path despite its extra plumbing)?
  ? Do you want to keep eu-north-1 for account-default consistency (everything in the runbook works there; -$1.50/month, +18 ms RTT, no App Runner) or switch the prototype to eu-central-1 as recommended?

############ v2:4726761704c0f9ba8da2d4678a68b609a02a6751518d5b8785ed12d446c5bc1d
SUMMARY: A reversible cotton-twill bucket hat is built as two complete self-fabric shells (each = 1 top circle + 2 side-band panels + 2 half-annulus brim panels), joined right-sides-together around the brim outer edge, turned through a 6-8 cm gap, and locked with the brim topstitch; there is no separate lining, no sweatband, and normally no metal eyelets. Verified adult sizing for the S/M/L test case is head 54-56/56-58/58-60 cm with finished inside circumference 57/59/61 cm (1 cm ease, 2 cm grade), crown height at side 8.5/9.0/9.5 cm, top-circle diameter ~17.4/18.0/18.6 cm and a 6.5 cm brim (brim outer circumference ~98/100/102 cm). Fabric: 100% cotton 3/1 twill 20x16 / 128x60, 270-280 gsm (~8.3 oz/yd²), 147-150 cm wide, sanforized (residual shrinkage <=3%), ~0.20 m of each colour per hat at 150 cm width. Sewing is 301 lockstitch on DBx1 Nm 80-90 with Tex 27-30 (Tkt 120/100) core-spun poly for seaming and Tex 40 (Tkt 75/80) on Nm 90-100 for the single brim topstitch at 6 mm from the edge, 10-12 SPI (Coats spec: 12-14 st/3 cm); needle thread khaki / bobbin black so each face shows its own colour. With only one topstitch row at 2 x 280 gsm the brim is soft, so spec a 50-70 gsm woven fusible on one brim (cut on bias, trimmed out of the seam allowance) or accept a packable brim. No published headwear SAM exists; an operation-based estimate is ~11-13 sewing SAM (~15 min incl. finishing/packing). Key tech-pack insight: the two stated colourways are physically the same hat unless the brand label side or a detail differentiates them.
RECOMMENDATIONS:
  - Generator spec chart for the test case (finished cm, S/M/L): inside circumference at brim seam 57/59/61 (+/-1.0), crown height at side 8.5/9.0/9.5 (+/-0.5), top-circle diameter 17.4/18.0/18.6 (+/-0.5), brim width 6.5/6.5/6.5 (+/-0.3), brim outer circumference 97.8/99.8/101.8 (+/-1.5), total height 15.0/15.5/16.0 (+/-0.5), topstitch 0.6 from edge (+/-0.1). State 'fits head 55-56 / 57-58 / 59-60 cm' on the size label.
  - Construction block: two full 280 gsm twill shells (1 top, 2 band, 2 brim halves each), 1 cm SA, shells joined RS-together at brim outer edge leaving 6-8 cm gap at CB, SA graded to 5 mm and notched, turned, pressed, single 301 topstitch at 6 mm closing the gap; add stitch-in-ditch at brim/band seam as recommended default. No lining, no sweatband, no eyelets, no strap - say so explicitly.
  - BOM: 100% cotton 3/1 twill 20x16 128x60, 280 gsm, 147-150 cm, sanforized <=3%, khaki + black, ~0.22 m each per hat; woven fusible 50-70 gsm on black brim only (optional, bias-cut); thread Tex 27-30 core-spun poly for seaming (khaki + black), Tex 40 core-spun for topstitch (khaki needle / black bobbin); woven loop label; heat-transfer or printed care label; hangtag; polybag.
  - Sewing notes: 301 lockstitch, DBx1 Nm 80-90 R for seaming, Nm 90-100 for topstitch, 10-12 SPI; DPx5 only if the factory uses an automatic brim stitcher; balance tension so no bobbin colour shows on either face; match band and brim seams across both shells; ease band to top circle with 4 notches.
  - Quality: run shrinkage and crocking tests on each colour lot before cutting (black onto khaki is the main risk); use AQL 2.5 major / 4.0 minor with the reversible-specific checklist above; approve a size-M sealed sample of each face before bulk.
  - Flag the colourway issue to the client: 'khaki outer/black reverse' and 'black outer/khaki reverse' are the same hat; decide whether to ship one SKU or differentiate by label side/hangtag.
  - Mark SAM figures in the output as estimates (~11-13 sewing min, ~15 total) pending a first-bundle time study; there is no published headwear SAM.
OPEN QUESTIONS:
  ? Is the brim intended to be soft/packable (no interfacing, single topstitch) or structured (add fusible on one brim and/or 3 rows at 6/12/18 mm)?
  ? Are the two colourways meant to be one SKU (same physical hat) or two SKUs differentiated by brand-label side / hangtag / thread colour?
  ? Which face carries the woven brand label and care/content label, and does the brand accept a printed/heat-transfer care label inside one crown (visible when reversed)?
  ? Egyptian labelling requirements (Arabic care/content label, country of origin) for a domestic first run - and is export (EU/US care-label rules) planned?
  ? Band construction preference: 2 side seams (aligns with 2-piece brim) or single CB seam?
  ? Confirm size label convention: S/M/L alpha only, or also print the cm range (55-56 / 57-58 / 59-60)?
  ? Fabric sourcing: is the 280 gsm twill already sourced (mill spec sheet with shrinkage/colourfastness available), or should the tech pack specify a target construction (20x16 / 128x60) for local Egyptian mills?
  ? Is ventilation required (embroidered eyelets on each shell) or acceptable to omit?
  ? Target order quantity and size ratio (affects marker efficiency and the consumption figure).

############ v2:ac341bce221c0fb55523bdf58df742944a371632b4dfb2a9cc12a741e427781a
SUMMARY: Factories consume a tech pack as a locked PDF (one section per page, sketch and its notes on the same page, numbered POMs tied to a diagram) plus an editable XLSX for the BOM and measurement chart; Excel-only packs drift between machines and are the format factories complain about. Egyptian/MENA makers (e.g. Kemora, Cairo, MOQ 50/colour; Thread Gate, MOQ 500) explicitly accept "photo, sketch, sample or tech pack", and Arabic-market tech-pack services deliver PDF/JPG/PNG with English section names transliterated or glossed in Arabic, so the winning format is English-primary with an Arabic gloss line per heading and per construction note, metric cm, Latin digits, LTR page. Of the export libraries, @react-pdf/renderer and raw PDFKit have no native BiDi (workarounds exist: U+200F markers, bidi-shaper), WeasyPrint is solid on Arabic via Pango/HarfBuzz but needs system libs, and headless Chromium (Playwright + @sparticuz/chromium or a container) gives correct Arabic shaping, @font-face embedding and the same HTML/SVG for web and PDF; exceljs (rightToLeft view, images, pageSetup) covers XLSX. For the sketch, a parametric inline-SVG template per product type (bucket hat: side, top, underside views with numbered POM leaders) is the only approach that guarantees symmetric, dimension-true, callout-anchored flats without CAD; AI vector models (Recraft V4 SVG) are fast but produce asymmetry and cannot be trusted to anchor POMs. Canonical bucket-hat numbers, thread/needle/SPI values, Pantone/hex for khaki and jet black, and a versioning/revision protocol are captured below.
RECOMMENDATIONS:
  - Export stack: author the tech pack once as HTML+CSS (@page A4 landscape, one section per page with break-after, print CSS) and render PDF with Playwright + headless Chromium (playwright-core + @sparticuz/chromium in a container-image Lambda at ≥1600 MB, or an ECS Fargate service if throughput is steady); await document.fonts.ready and networkidle before page.pdf({format:'A4', preferCSSPageSize:true, printBackground:true, tagged:true, outline:true}). Same HTML powers the web preview, so the PDF and the web page cannot diverge.
  - Do not build the bilingual PDF on @react-pdf/renderer or raw PDFKit; if a pure-Node no-browser path is ever required, use PDFKit + bidi-shaper; if the team is Python, WeasyPrint ≥66 in a Debian container is the acceptable alternative (Pango handles BiDi), but drop inline JS/SVG interactivity.
  - XLSX: exceljs, one workbook per tech pack with sheets Cover, BOM, POM_Spec (graded S/M/L + tolerance + factory-comment column with data-validation dropdown 'OK / Adjust / Query'), Construction, Colorways, Revision_Log; set pageSetup {paperSize:9 (A4), orientation:'landscape', fitToPage:true}, embed the flat as PNG via addImage on the POM sheet, and offer a toggle that sets views=[{rightToLeft:true}] only for an Arabic-first factory (default LTR, since POM codes and cm values are Latin).
  - Bilingual scheme: English primary, Arabic gloss on the same line or directly beneath for every section heading, POM description, construction step and BOM item (e.g. 'Head circumference at sweatband seam / محيط الرأس عند خط الحزام'); page direction stays LTR, each Arabic span gets dir='rtl' lang='ar' and unicode-bidi:isolate; numbers, units (cm, gsm, Tex, SPI) and POM codes always Latin digits; fonts: Inter (Latin) + Noto Sans Arabic or Cairo (Arabic) at +1 pt, bundled as local TTF/WOFF2 via @font-face in the container image, never fetched from Google at render time.
  - Page structure for v1: 1 Cover (style#, name, brand, season, version, date, colorway thumbnails, contact); 2 Product description & intended use + reference photo; 3 Technical flats with construction callouts (same page as notes); 4 How-to-measure diagram with numbered POMs; 5 Graded spec table S/M/L with tolerance; 6 BOM; 7 Colorways (swatch, Pantone TCX, hex, DTM thread, which shell is 'outer'); 8 Labels & packaging; 9 Revision log + sample-comment table (Round, Sample type proto/fit/PP, Date, Comment, Action, POM affected).
  - Bucket hat POM set (codes reused as SVG anchor ids): A Internal head circumference at crown/brim seam (S 56 / M 58 / L 60 cm, ±0.5); B Crown side height, seam to seam at side seam (9.0 / 9.0 / 9.5 cm, ±0.3); C Brim width, crown seam to finished edge at CF (6.5 / 6.5 / 6.5, ±0.3); D Tip length, seam to seam (18.5 / 19.2 / 19.9, ±0.3); E Tip width (16.5 / 17.2 / 17.9, ±0.3); F Brim outer circumference (≈A + 2π·C: 96.8 / 98.8 / 100.8, ±1.0); G Brim topstitch distance from edge (0.6 cm, ±0.1); H Side-seam topstitch offset (0.6 cm); J Crown/brim seam edgestitch (0.2 cm, visible both faces); K Label position, CB inside, 1.5 cm below tip seam. Grade 2 cm on A, ~0.7 cm on D/E per size.
  - SVG strategy: build one parametric template module per product type (start: bucket hat) that takes the M-size POM values and emits three views as inline SVG: (1) side elevation — tip seam line, trapezoidal side band, brim as flared band with a single dashed topstitch line at 6 mm; (2) top plan — tip oval (D×E), crown ring, brim outer ring, side seams marked at 3/9 o'clock; (3) underside — brim, crown opening, CB label rectangle, closing edgestitch. Each POM is an <g id='pom-A'> with a leader line and a numbered circle badge; stroke 1.2 px black outlines, 0.6 px construction lines, dashes for topstitch; no fills; viewBox in mm so 1 unit = 1 mm and the same SVG drops into the HTML/PDF and rasterises to PNG for the XLSX via Playwright screenshot. Generate the diagram from data, not from the model's prose.
  - Use AI image generation only for the optional 'appearance' panel (photo-to-line-art of the uploaded product photo, or Recraft V4 SVG with the prompt 'technical flat sketch of a reversible bucket hat, side view, clean closed outlines, no fill, black lines on white, symmetrical'); run a symmetry check (mirror-and-diff on the raster) and fall back to the parametric flat if it fails; never attach POM leaders to an AI image.
  - Sketch-input handling: when the user uploads a photo, have the LLM extract structured attributes (crown height class, brim width class, panel count, topstitch rows, closure/adjuster) into the parametric template's parameters, and show the annotated photo next to the generated flat so the factory sees intent and spec side by side.
  - Versioning: expose file names as <BRAND>-<STYLE#>_<COLORWAY>_TP_v<NN>_<YYYY-MM-DD>.pdf/.xlsx, embed version + date in every page footer, keep immutable copies per version in S3, and implement 'Sample review' as a form that appends dated, quantified comments and bumps v1.x → v1.(x+1) (v2 only for construction changes); include the sample-type label (Proto / Fit / PP) to print on the sample bag.
  - Construction page for the canonical hat: 1 cm SA throughout; 301 lockstitch Tex 40 DTM, Nm 90/14, 10 SPI seams / 8–9 SPI topstitch; crown side seams pressed open and topstitched 6 mm both sides on each shell; tip attached with 0.9 cm ease stitch, SA toward side, edgestitched 2 mm; brims joined RS together, graded, turned, single row topstitch 6 mm from edge (per brief; note in comments that 3–5 concentric rows at 12 mm is the common alternative if the brim flops); crowns bagged to brim seam with 10 cm turning gap, closed with a 2 mm edgestitch that shows on both faces; DTM thread per shell with grey Tex 40 bobbin at the junction seam if the factory cannot switch.
  - Ask the factory for confirmation in the pack itself: a 'Factory acknowledgement' block (sample size, unit = cm, tolerance accepted, thread available, lead time) with an Arabic gloss, and a QR link back to the live web version so revisions are pulled, not emailed.
OPEN QUESTIONS:
  ? Which Egyptian workshops will run the first sample (in-house CMT, a Cairo private-label maker like Kemora, or a QIZ factory)? Their sample-room language mix (Arabic-only cutters/sewers vs English-reading merchandisers) decides whether the Arabic gloss must cover construction steps or only headings.
  ? Does the marketplace need XLSX at all for v1, or is a PDF plus a web 'comment on this POM' flow enough? XLSX doubles the export surface.
  ? Runtime constraint on AWS: is a container-image Lambda (>250 MB) or a Fargate service acceptable, or must rendering stay inside a zip-deployed Lambda? This decides Chromium vs PDFKit+bidi-shaper.
  ? Node or Python for the render service? WeasyPrint is the strongest no-browser Arabic option but only in Python.
  ? Should the canonical hat spec a single brim topstitch row exactly as briefed, or allow the generator to propose the 3–5 row alternative when the brim is unlined/uninterfaced?
  ? Do you want the generator to emit both colorways as two tech packs (one style# each) or one style with two colorway lines (the reversible construction is identical, only the 'outer' designation flips)?
  ? Which base size do you want as the sample size for grading (M / 58 cm is the industry default)?
  ? Is there budget for Pantone TCX swatch references in the BOM, or should the colour standard be 'match to supplied lab dip / hex' for a first local run?

############ v2:1d1c8597587986066e62714c083e02ce3701e60ddd6aa5ae32573bf73540a247
SUMMARY: The 2025-26 "AI tech pack" field splits into three tiers: (1) incumbent spec/PLM tools with no generative AI (Techpacker $35-125/user/mo, Sewport, Fashinza managed service, Audaces Idea with auto pre-costing), (2) 3D suites that auto-derive POM/BOM from a modeled garment but require 3D expertise (CLO-SET Excel tech pack at $50/mo; Browzwear Excel/HTML/DXF/CSV export, enterprise pricing), and (3) a crowded wave of photo/text-to-pack startups (The New Black $15-195/mo, aitechpacks.com $18/pack or $48/mo, Adstronaut $3-6/pack, Genpire ~40-50 credits/pack on $22.90-74.90/mo, The F* Word 115 credits/pack on $299/mo, Skema3D, Tchpack "Aria", Alibaba Accio/AI Mode). Every tier-3 tool produces "plausible" AI measurements with an explicit disclaimer that fit intent, tolerances and suppliers must be corrected by a human; independent reviewers put draft completeness at 50-70%, none validates against a factory, almost all export PDF only (Excel only on higher tiers), none supports Arabic, and several explicitly say accessories/hats need manual supplementation. Public hat prior art (Specter OS cap guide, FittDesign 6-panel bucket hat template, Ace Headgear/Purnaa manufacturer templates, Sumkcaps/Yukaihe/Solbari size charts) converges on a 5-10 page layout: cover, multi-view flat, POM table with target + tolerance (±0.5 cm crown/brim, ±1 cm circumference), BOM with gsm/Pantone TCX/thread Tex, construction with SPI and brim row spacing, colorway matrix, labels/packaging. A prototype that computes (not guesses) the bucket-hat geometry from head circumference, states every assumption with a confidence flag, does consumption math per colour, emits PDF + XLSX + JSON and renders in Arabic and English will be differentiated from everything surveyed.
RECOMMENDATIONS:
  - Derive, do not guess: make head circumference the single driver and compute crown-top diameter (C/pi), side-band length, brim inner/outer radii and every graded value for S/M/L from it; print the formula next to each derived number. No surveyed tool does this; all of them emit 'plausible' numbers and disclaim fit.
  - Ship a hat-specific POM set with targets AND tolerances: inner head circumference (±1 cm), crown top diameter, side height/crown depth (±0.5), brim width (±0.5), total brim diameter, topstitch distance from edge (±1 mm), eyelet Ø/position if any; state measurement method text for each POM (Sewport 5-column convention). Evaluators will check whether S/M/L grade by a consistent 2 cm and whether crown diameter is consistent with circumference.
  - Include an operation-sequenced construction sheet (numbered ops, stitch type 301, SPI 8, SA 1 cm, Tex 40 tonal thread, needle 90/14, which colour thread on which side) with the reversible bag-out logic explicit and the single brim topstitch row dimensioned (e.g. 8 mm from edge). Most AI tools output prose 'construction notes'; a real op sheet is the differentiator a factory recognises.
  - Do the consumption and costing math visibly: net piece area per colour, marker efficiency assumption, metres per hat per colour at the stated fabric width, per-run totals with shrinkage/waste allowance, fabric grams per hat, and a cost-sheet skeleton (fabric, thread, labels, CM, packaging). Only Audaces and the 3D suites do this and none of the photo-to-pack tools do.
  - Model colorways as a matrix, not a note: for a reversible hat each colorway row lists outer shell, inner shell, brim top, brim underside, thread, label, with Pantone TCX + hex (16-0726 TCX #A39264, 19-0303 TCX #2D2C2F), and the BOM must roll up fabric quantity per colour across both colorways (symmetric here) so the buyer can order fabric.
  - Exports: PDF (A4, print-safe, RTL-aware), XLSX with real tables (POM grid with formulas so editing M regrades S/L; BOM with qty x price), and a machine-readable JSON of the whole pack for the MASDR RFQ flow. Excel is gated behind $48-130/mo tiers elsewhere; offering all three free in the prototype is a visible win.
  - Editability plus provenance: every field editable in the UI, and every AI-inferred value tagged with a confidence level (measured from image / inferred from category default / user-stated) plus an 'Assumptions & open questions for the factory' page. No competitor exposes per-field confidence; The New Black and aitechpacks bury a one-line disclaimer.
  - Render the flat as a parametric SVG (crown circle + side band + brim ring, front and side views, plus an exploded pattern-piece view) whose leader lines are bound to the POM values, rather than an AI raster; this makes the drawing consistent with the numbers by construction and prints crisply in both PDF and XLSX.
  - Bilingual AR/EN side-by-side (or toggle) with correct RTL layout, Arabic operation names (e.g. حياكة زخرفية = topstitch, بدل الخياطة = seam allowance, محيط الرأس = head circumference) and units in cm first; this is absent from Accio, CALA, The New Black and every other tool and matches MASDR's built-in translation positioning.
  - Add a factory-readiness checklist derived from the top rejection causes (missing tolerances, colour by name only, no thread/SPI, no label/packaging spec, no QC criteria) and score the pack; include a QC/inspection block (AQL 2.5, head-form circumference check, metal detection) so the evaluator sees the pack close the loop with production.
  - Benchmark visibly against the field in the submission: a one-page table showing that The New Black/aitechpacks/Adstronaut/Genpire/Accio output PDF-only 'plausible' specs with no consumption, no confidence flags, no Arabic, and note that hats are explicitly out of scope for Skema3D/Specter/aitechpacks.
OPEN QUESTIONS:
  ? Which target fit does the brand want: standard 3-size run centred on M = 57 cm head (S 55 / M 57 / L 59 finished inner) or an oversized streetwear block (Everybody.World style 58/61/64 cm)?
  ? Brim width and crown depth targets: classic 6.5-7 cm brim with 9.5 cm side, or short 5 cm 'Y2K' brim / 11-12 cm deep crown? Should S/M/L share brim and depth (typical) or grade them?
  ? Crown top: plain round crown (2 side panels + top circle) or a 6-panel crown as in most OEM blanks? This changes piece count, seam ops and consumption.
  ? Is the single brim topstitch row an edge-stitch (2 mm) or one row set in (e.g. 8 mm), and is the thread tonal to each side or one colour throughout?
  ? Eyelets, chin cord, drawstring adjuster or size label: include any? Where should the woven/care label sit on a reversible hat (one side only, or a loop label at the brim seam)?
  ? Fabric source: imported 280 gsm twill at 147 cm width vs a local Egyptian mill (width and shrinkage may differ) - which width should the consumption math assume, and is it pre-shrunk?
  ? Run quantity and colorway split (e.g. 300 pcs, 50/50 khaki-out vs black-out, size ratio 1:2:1) so consumption and cost totals can be stated?
  ? Units and language priority for the factory: cm-only or cm+inch, and Arabic-primary or English-primary page order?
  ? Does the evaluator expect a cost sheet (FOB/EXW estimate) in the pack, or only BOM quantities?
